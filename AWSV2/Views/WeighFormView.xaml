<UserControl
    x:Class="AWSV2.Views.WeighFormView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:AWSV2.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{StaticResource PrimaryHueDarkBrush}" FocusVisualStyle="{x:Null}">
        <ContentControl
            Content="{Binding WeighFormGrid}"
            FocusVisualStyle="{x:Null}"
            Focusable="False" />
        <ContentControl
            Content="{Binding WeighFormStackPanel}"
            FocusVisualStyle="{x:Null}"
            Focusable="False" />
    </Grid>

</UserControl>
